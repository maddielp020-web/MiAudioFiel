// ==================== MODO RÁPIDO (PLACEHOLDER) ====================
// Este archivo está reservado para futura implementación de TTS ligero
// (ej. voces locales, Web Speech API, etc.)

/**
 * Función placeholder para generar audio rápido
 * @param {string} text - Texto a convertir
 * @returns {Promise<Blob|null>} - Siempre null por ahora
 */
export async function generarAudioRapido(text) {
  console.log('⚡ Modo Rápido: no implementado aún');
  return null;
}

// ==================== LOGS DE VERIFICACIÓN ====================
console.log('✅ modo-rapido.js cargado (placeholder)');
